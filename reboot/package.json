{
  "name": "uaforce-reboot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "node --experimental-strip-types --test tests/*.test.ts",
    "build:release": "python3 tools/prepare-release.py && tsc --noEmit && vite build --mode release && python3 tools/check-release.py",
    "preview:release": "vite preview --outDir dist-release --host 127.0.0.1"
  },
  "dependencies": {
    "peerjs": "1.5.5",
    "playcanvas": "2.22.1"
  },
  "devDependencies": {
    "typescript": "~5.9.3",
    "vite": "8.2.2"
  }
}
